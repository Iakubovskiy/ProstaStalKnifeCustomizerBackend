using Microsoft.AspNetCore.Identity;
using System.ComponentModel.DataAnnotations;

namespace WorkshopBackend.Models
{
    public class User : IdentityUser
    {
        // probably will add something in future
    }
}
